document.getElementById('registerForm')?.addEventListener('submit',async e=>{e.preventDefault();const f=e.currentTarget;const msg=document.getElementById('formMessage');const data=Object.fromEntries(new FormData(f));data.edad=Number(data.edad);const r=await fetch('/registro/paciente',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();if(!r.ok){showMessage(msg,j.mensaje||'No se pudo crear la cuenta');return}showMessage(msg,'Cuenta creada correctamente. Ahora podés iniciar sesión.','ok');f.reset();setTimeout(()=>window.location.href='/login',700);});
