async function loadMedico(){const box=document.getElementById('medicoConsultas');const r=await fetch('/consultas/pendientes');if(!r.ok){box.innerHTML='<div class="empty">No se pudieron cargar las consultas.</div>';return}const data=await r.json();box.innerHTML=data.length?data.map(c=>`<article class="doctor-item"><h3>Consulta #${c.id_consulta}</h3><p><strong>Estado:</strong> ${escapeHtml(c.estado)}</p><p><strong>Fecha:</strong> ${escapeHtml(c.fecha_hora)}</p><p>${escapeHtml(c.descripcion_sintomas)}</p><div class="doctor-actions"><button class="btn btn-primary" onclick="responderDemo(${c.id_consulta})">Responder</button></div></article>`).join(''):'<div class="empty">No hay consultas pendientes.</div>';}
async function responderDemo(id){const descripcion=prompt('Escribí la respuesta para el paciente:');if(!descripcion)return;const turno=prompt('Turno presencial (opcional):')||null;const r=await fetch('/devoluciones/'+id,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({descripcion,turno})});const j=await r.json();alert(j.mensaje|| (j.ok?'Respuesta enviada':'Ocurrió un error'));if(r.ok)loadMedico();}
document.getElementById('recetaForm')?.addEventListener('submit',async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/recetas',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();showMessage(document.getElementById('recetaMessage'),j.ok?'Receta guardada correctamente.':j.mensaje,j.ok?'ok':'error');if(r.ok)e.currentTarget.reset();});
loadMedico();
